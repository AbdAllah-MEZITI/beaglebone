config BR2_PACKAGE_LEDFLASH
	bool "Led flash module"
#	default y
#	depends on BR2_LINUX_KERNEL
	help
		Kernel module to support LED flash.

#comment "LED flash module requires a linux kernel to be built"
#	depends on !BR2_LINUX_KERNEL
